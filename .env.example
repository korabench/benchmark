AI_GATEWAY_API_KEY=

# OpenAI-compatible providers (optional). Set when you want to use a slug like
# `vllm/<model-id>` as the target, judge, or user model.
# VLLM_BASE_URL=http://localhost:8000/v1
# VLLM_API_KEY=EMPTY
